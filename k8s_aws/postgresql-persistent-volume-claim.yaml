apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: trade-service-postgres-pvc
  namespace: globeco
  labels:
    app: trade-service-postgres
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: ebs-gp3
  resources:
    requests:
      storage: 10Gi
